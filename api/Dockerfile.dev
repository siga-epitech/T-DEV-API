FROM node:18-alpine

# Installer les outils réseau (ping)
RUN apk add --no-cache iputils

WORKDIR /api

# Vérification de la connexion réseau
RUN ping -c 4 google.com

COPY package*.json ./
RUN npm install -g npm@latest && npm install --verbose
COPY . .

EXPOSE 3000

CMD ["npm", "start"]
